<img
  :src="imgDir(this.src)"
  :alt="this.alt?.trim()"
  :sizes="$data.image[this.sizes] || this.sizes || 'auto'"
  :width="this.width"
  :loading="this.loading || 'lazy'"
  :decoding="this.decoding || 'async'"
  @attributes="this.attrs"
>
