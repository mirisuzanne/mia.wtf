<h1
  id="title"
  @html="mdI(this.title)"
></h1>

<post-meta>
  <date-time
    webc:if="this.isPost"
    :date="this.date"
  ></date-time>
  <span webc:if="this.isPost && this.subtitle">–</span>
  <roll-if
    :@content="this.subtitle"
  ></roll-if>
</post-meta>

<style webc:bucket="layout">
  post-meta {
    font-family: var(--ui-font);

    & p {
      display: inline;
      font-style: italic;
    }
  }
</style>
